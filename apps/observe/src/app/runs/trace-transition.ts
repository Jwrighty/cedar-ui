/**
 * Session-storage key holding the id of the feed row a trace overlay was opened
 * from, so focus can be restored to it when the overlay closes.
 */
export const TRACE_ORIGIN_ROW_STORAGE_KEY = "observe-origin-trace-row";
